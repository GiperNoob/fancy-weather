import React from 'react';
import './degrees.css';

const Degrees = () => {
  return (
    <div className="degrees">

      <button className="btn btn_celsius">C</button>
      <button className="btn btn_fahrenheit">F</button>

    </div>
  )
};

export default Degrees;
